# 技术愿景

最后更新时间：2026-04-03
轮次：#20

## 架构评判

项目已经完成从“单文件原型”到“可部署小型商城”的架构演进，当前关键技术目标均已落地：

- 已完成前后台路由统一，入口清晰
- 已完成后台渐进式 MVC 拆分
- 已完成前台事件委托统一，减少内联脚本
- 已完成 CSS 组件化与移动端基础适配
- 已完成安装向导、双模邮件、统一日志、错误页、目录保护等基础设施

当前架构在“低成本部署、低依赖、易理解、易迁移”这个约束下是合理且成熟的。

## 如果只能做三件事

历史上最关键的三件技术工作已全部完成：

1. 完成前后台结构化拆分，解决代码继续演进的可维护性问题
2. 完成统一安全基线，包括 CSRF、上传校验、日志、限流、目录保护、404/错误页
3. 完成部署链路闭环，包括 install.php、schema.sql、配置模板、README、安装锁

## 架构演进路线

四阶段演进已经全部完成：

1. 第一阶段：把原始页面跑通，形成最小可用商城
2. 第二阶段：拆分后台 MVC 与公共工具层
3. 第三阶段：统一前台交互、搜索、订单、SEO、移动端
4. 第四阶段：部署就绪与最终安全加固

## 已达成的创新点

- 零框架原生 PHP 商城，适合廉价虚拟主机
- 安装向导一体化完成“建表 + 配置 + 建管理员”
- 邮件发送采用 SMTP socket 优先、`mail()` 回退的双模式方案
- 统一日志工具 `shop_log()`，默认写入 `logs/app.log`，失败时回退 `error_log()`
- 前台事件委托统一到 `data-*` 属性，降低模板与脚本耦合

## Round 19-20 安全加固记录

- 安装器增加 BOM 防护与 prefix 输入校验
- 安装入口统一为 `install.php`，`admin/setup.php` 仅保留重定向
- checkout 创建订单状态修正为 `pending`
- 目录封禁扩展到 `database/` 和 `logs/`
- 后台登录新增 5 次失败锁定 15 分钟机制，并记录警告日志
- 未知路由统一返回 HTTP 404 友好错误页

## 剩余运维需求

当前剩余工作不再属于代码实现，而是部署与运维执行：

1. Ops-1：生产 SMTP 联调与可达性验证
2. Ops-2：日志采集、轮转、监控与告警
3. UX-3：375px / 414px / 768px 真机测试与问题回归
